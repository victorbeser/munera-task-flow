// Exemplo: stdout e stderr aparecem no arquivo de log e no SSE.
console.log('Olá do JavaScript!');
console.error('Mensagem de diagnóstico (não é necessariamente uma falha).');
setTimeout(() => console.log('Fim do exemplo.'), 2000);
